<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @artist.name %>
</p>

<p>
		<strong>Songs That Belong to Artist:</strong>
		<% @artist.songs.each do |song| %>
		<ul>
				<li><%= song.name  %></li>
		</ul>
		<% end %>
</p>
<%= link_to 'Edit', edit_artist_path(@artist) %> |
<%= link_to 'Back', artists_path %>
